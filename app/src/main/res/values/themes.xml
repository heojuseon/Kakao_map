<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Kakao_map" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Kakao_map" parent="Base.Theme.Kakao_map" />

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <item name="postSplashScreenTheme">@style/Theme.Kakao_map</item>

    <!-- api 31+ (12 이상) -->
<!--        <item name="windowSplashScreenBackground">@color/black</item>-->
<!--        <item name="android:windowSplashScreenAnimatedIcon"></item>-->
    </style>
</resources>